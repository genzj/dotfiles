# https://starship.rs/config/
# based on https://starship.rs/presets/pastel-powerline

"$schema" = 'https://starship.rs/config-schema.json'

format = """
[](#9A348E)\
$os\
$username\
[](fg:#9A348E bg:black)\
$shlvl\
[](bg:#DA627D fg:prev_bg)\
$directory\
[](fg:#DA627D bg:#FCA17D)\
$git_branch\
$git_status\
[](fg:#FCA17D bg:#33658A)\
$status\
[ ](fg:#33658A)\
"""

right_format = """
[$all](fg:#aaaa77)\
"""
# A continuation prompt that displays two filled-in arrows
continuation_prompt = '▶▶ '

# Disable the blank line at the start of the prompt
# add_newline = false

# You can also replace your username with a neat symbol like   or disable this
# and use the os module below
[username]
show_always = true
style_user = "bg:#9A348E"
style_root = "bg:#9A348E"
format = '[$user ]($style)'
disabled = false

# An alternative to the username module which displays a symbol that
# represents the current operating system
[os]
style = "bg:#9A348E"
disabled = true # Disabled by default

[directory]
style = "bg:#DA627D"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
# Keep in mind that the order matters. For example:
# "Important Documents" = " 󰈙 "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important 󰈙 " = " 󰈙 "

[c]
symbol = " "
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[docker_context]
symbol = " "
style = "bg:#06969A"
format = '[ $symbol $context ]($style)'

[elixir]
symbol = " "
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[git_branch]
symbol = ""
style = "bg:#FCA17D"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "bg:#FCA17D"
format = '[$all_status$ahead_behind ]($style)'

[golang]
symbol = " "
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[gradle]
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[nim]
symbol = "󰆥 "
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[scala]
symbol = " "
style = "bg:#86BBD8"
format = '[ $symbol ($version) ]($style)'

[time]
disabled = false
time_format = "%R" # Hour:Minute Format
# style = "bg:#33658A"
style = "fg:#3395EA"
format = '[ ♥ $time ]($style)'

[[battery.display]] # 'bold red' style and discharging_symbol when capacity is between 0% and 10%
threshold = 15
style = 'bold red'

[[battery.display]] # 'bold yellow' style and 💦 symbol when capacity is between 10% and 30%
threshold = 40
style = 'bold yellow'

[[battery.display]] # 'bold yellow' style and 💦 symbol when capacity is between 10% and 30%
threshold = 101
style = 'bold green'

[shlvl]
disabled = false
style = 'fg:bold yellow bg:black'
format = '[$symbol $shlvl]($style)'
symbol = '󰼢'
threshold = 2

[character]
disabled = true

[status]
disabled = false
style = "bg:#33658A"
format = '[$symbol $status]($style)'

{{ if .attribute.is_amzn -}}
[custom.midway_overdue]
shell = "/bin/zsh"
command = '$HOME/zim-amzn-dev/scripts/prompt_my_midway_lifetime'
when = '$HOME/zim-amzn-dev/scripts/test_my_midway_lifetime 0 1'
format = '[$symbol $output ]($style)'
style = 'bold red'
symbol = '󱕴'

[custom.midway_short]
shell = "/bin/zsh"
command = '$HOME/zim-amzn-dev/scripts/prompt_my_midway_lifetime'
when = '$HOME/zim-amzn-dev/scripts/test_my_midway_lifetime 1 300'
format = '[$symbol $output ]($style)'
style = 'bold yellow'
symbol = '󱕴'

[custom.midway_mid]
shell = "/bin/zsh"
command = '$HOME/zim-amzn-dev/scripts/prompt_my_midway_lifetime'
when = '$HOME/zim-amzn-dev/scripts/test_my_midway_lifetime 300 3600'
format = '[$symbol $output ]($style)'
style = 'bold blue'
symbol = '󱕴'

[custom.midway_long]
shell = "/bin/zsh"
command = '$HOME/zim-amzn-dev/scripts/prompt_my_midway_lifetime'
when = '$HOME/zim-amzn-dev/scripts/test_my_midway_lifetime 3600 900000'
format = '[$symbol $output ]($style)'
style = 'green'
symbol = '󱕴'

{{ end -}}

[custom.cpu_arch]
shell = "/bin/zsh"
command = '$HOME/.local/share/zim-my-config/scripts/prompt_cpu_arch'
when = true
format = '[$symbol $output ]($style)'
style = 'blue'
symbol = '󰘚'
